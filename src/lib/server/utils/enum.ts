export enum MessageResponse {
  Error = "error",
  Success = "success",
  VerifyEmail = "verify_email"
}

export enum UserType {
  User = "user",
  Guest = "guest",
  Customer = "customer"
}

export enum UserRole {
  User = "user",
  Admin = "admin"
}